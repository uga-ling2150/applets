(function(){
'use strict';
const $=id=>document.getElementById(id), clips=window.BC_CLIPS, core=window.BC_CORE, audio=$('audio');
let personalRuns=null, priorExposure=false;
let clip=clips[0], runs=[], active=null, revealed=false, ready=false, buffering=false, reviewUntil=null, lastSave=-1;
const initialRoom=new URLSearchParams(location.hash.slice(1)).get('teacher-room')||new URLSearchParams(location.hash.slice(1)).get('room')||new URLSearchParams(location.search).get('room');
const key=()=>`ling2150-7a:${window.BC_CLASSROOM?.identityNamespace||window.BC_CLASSROOM?.room||initialRoom||"offline"}${window.BC_CLASSROOM?.mode==='teacher'||(!window.BC_CLASSROOM?.room&&new URLSearchParams(location.hash.slice(1)).has('teacher-room'))?':teacher':''}:${clip.id}`;
const time=t=>`${Math.floor(t/60)}:${String(Math.floor(t%60)).padStart(2,'0')}`;
const uid=()=>globalThis.crypto?.randomUUID?.()||`run_${Date.now()}_${Math.random().toString(36).slice(2)}`;
function log(type){if(active?.practice&&!['practice_start','practice_complete'].includes(type))return;window.dispatchEvent(new CustomEvent('bc-action',{detail:{type,attemptId:active&&!active.practice?active.id:null,position:Number.isFinite(audio.currentTime)?+audio.currentTime.toFixed(3):null}}));}
function say(s){if($('status').textContent!==s)$('status').textContent=s;}
function error(s){$('error').textContent=s;}
function persist(){if(window.BC_CLASSROOM?.readOnly||window.BC_CLASSROOM?.mode==='teacher')return;try{localStorage.setItem(key(),JSON.stringify({runs:personalRuns||runs,revealed,priorExposure,reflection:$('reflection').value,active:active&&!active.practice?{...active,position:audio.currentTime}:null}));if($('save-status').textContent!=='Saved on this browser. Download a copy before leaving or clearing browser data.')$('save-status').textContent='Saved on this browser. Download a copy before leaving or clearing browser data.';}catch{$('save-status').textContent='Browser saving is unavailable. Keep this page open and download completed results.';}}
function controls(){
 const cloud=window.BC_CLASSROOM||{},busy=!!active;const locked=cloud.mode==='teacher'||cloud.initializing||(cloud.mode==='student'&&(cloud.submitting||cloud.closed||cloud.readOnly));
 $('class-view').disabled=busy||!cloud.released;
 $('clip').disabled=busy||cloud.initializing||cloud.loading||cloud.submitting; $('start').disabled=busy||locked||!ready||runs.length>=core.MAX_RUNS;
 $('start').textContent=cloud.mode==='student'?(runs.length||cloud.submitted?'Listen again':'Start listening'):`Start participant ${runs.length+1}`;
 $('practice').disabled=busy||locked||!ready;
 $('mark').disabled=cloud.readOnly||!busy||audio.paused||buffering||!ready;
 $('pause').disabled=cloud.readOnly||!(busy||reviewUntil!==null)||!ready;
 $('pause').textContent=audio.paused?'Resume':'Pause';
 $('undo').disabled=cloud.readOnly||!busy||!active.marks.length;
 $('discard').disabled=cloud.readOnly||!busy;
 $('reveal').disabled=busy||!runs.length; $('reveal').hidden=cloud.mode==='student'||cloud.mode==='teacher'; $('download').disabled=busy||!runs.length; $('csv').disabled=busy||!runs.length; $('import').disabled=busy||!!cloud.room;
 $('clear').disabled=busy||!!cloud.room;
 const own=cloud.mode==='student'&&personalRuns===null;$('saved-count').textContent=`${runs.length} completed ${own?'attempt':'participant'}${runs.length===1?'':'s'}.`;
 const marks=active?active.marks.length:(personalRuns||runs).at(-1)?.marks.length||0;$('count').textContent=`${marks} click${marks===1?'':'s'} ${active?'in this round':'in your latest round'}.`;
 document.querySelectorAll('[data-replay]').forEach(b=>b.disabled=busy||!ready);
}
function load(){
 audio.pause();reviewUntil=null;ready=false;buffering=false;active=null;personalRuns=null;runs=[];revealed=false;priorExposure=false;error('');$('retry-audio').hidden=true;$('reflection').value='';$('import-status').textContent='';$('transcript-details').open=false;
 try{const s=window.BC_CLASSROOM?.mode==='teacher'||(!window.BC_CLASSROOM&&location.hash.includes('teacher-room'))?null:JSON.parse(localStorage.getItem(key())||(!window.BC_CLASSROOM?.room&&!initialRoom&&localStorage.getItem(`ling2150-7a:${clip.id}`))||'null');if(s){runs=core.merge([],{format:'ling2150-7a',version:1,clipId:clip.id,duration:clip.duration,runs:s.runs},clip);revealed=s.revealed===true;priorExposure=!!s.priorExposure||revealed||runs.length>0;$('reflection').value=typeof s.reflection==='string'?s.reflection.slice(0,4000):'';if(s.active){const a=s.active;core.validateRun({...a,complete:true},clip);if(Number.isFinite(a.position)&&a.position>=0&&a.position<clip.duration)active={...a,practice:false};}}}catch{error('Saved work could not be read. You can import a previously downloaded results file.');}
 $('context').textContent=clip.context;$('progress').max=clip.duration;$('progress').value=0;$('clock').textContent=`0:00 / ${time(clip.duration)}`;
 $('source').textContent=`${clip.title}: meeting ${clip.meeting}, speaker ${clip.speaker}, original ${clip.sourceStart.toFixed(2)}–${clip.sourceEnd.toFixed(2)} seconds. ${clip.id.includes('ib4010')?'The original conversation contains a listener’s “mm-hmm” near 57.35 seconds in this clip; it may be audible through microphone bleed.':'No other speakers’ words are annotated in this interval; nonverbal sounds or microphone bleed may still occur.'}`;
 renderTranscript();renderComparison();$('round-title').textContent=active?'Unfinished round — resume when ready':'Ready to listen';
 audio.src=clip.src;audio.load();say('Loading recording…');controls();
}
function renderTranscript(){const list=$('transcript');list.replaceChildren();clip.transcript.forEach(s=>{const li=document.createElement('li'),b=document.createElement('button');b.className='btn-uga-outline';b.type='button';b.textContent=time(s.start);b.setAttribute('aria-label',`Replay from ${s.start.toFixed(1)} seconds`);b.dataset.replay='true';b.onclick=()=>replay(s.start,Math.min(clip.duration,s.end+1));li.append(b,document.createTextNode(s.text));list.append(li);});}
function renderComparison(){
 const own=window.BC_CLASSROOM?.mode==='student'&&personalRuns===null;$('comparison').hidden=!revealed||!runs.length;$('comparison-title').textContent=own?'Your listening choices':'Compare your class'; $('comparison-note').textContent=own?'Your clicks are shown below. There is no single correct answer.':`${runs.length} participants. Look for places where listeners agree or differ.`;
 const svg=$('wave');svg.replaceChildren();const ns='http://www.w3.org/2000/svg';const path=document.createElementNS(ns,'path');path.setAttribute('d',clip.peaks.map((v,i)=>`M${i*2+1} ${45-v*42}v${v*84}`).join(' '));path.setAttribute('stroke','#555');svg.append(path);$('axis-end').textContent=`${clip.duration.toFixed(1)} seconds`;
 $('rows').replaceChildren();runs.forEach((r,i)=>{const row=document.createElement('div');row.className='bc-row';const title=document.createElement('h4');title.textContent=own?`Your attempt ${i+1} · ${r.marks.length} click${r.marks.length===1?'':'s'}`:`Participant ${r.participant||i+1} · ${r.marks.length} clicks`;row.append(title);if(r.exposed){const note=document.createElement('p');note.className='bc-hint';note.textContent='Prior listening, practice, transcript or comparison may have influenced this attempt.';row.append(note);}const track=document.createElement('div');track.className='bc-track';track.setAttribute('aria-hidden','true');const list=document.createElement('div');list.className='bc-time-list';for(const t of r.marks){const marker=document.createElement('span');marker.className='bc-triangle';marker.style.left=`${t/clip.duration*100}%`;marker.textContent='▲';track.append(marker);const b=document.createElement('button');b.type='button';b.textContent=`${t.toFixed(2)} s`;b.setAttribute('aria-label',`Replay participant ${r.participant||i+1} marker at ${t.toFixed(2)} seconds`);b.dataset.replay='true';b.onclick=()=>replay(Math.max(0,t-2),Math.min(clip.duration,t+4));list.append(b);}row.append(track,list);$('rows').append(row);});
 $('bins').replaceChildren();core.bins(runs,clip.duration).forEach((n,i)=>{if(!n)return;const tr=document.createElement('tr');const a=document.createElement('td'),b=document.createElement('td');a.textContent=`${i}–${Math.min(i+1,clip.duration).toFixed(1)} s`;b.textContent=`${n} / ${runs.length}`;tr.append(a,b);$('bins').append(tr);});controls();
}
async function play(){try{await audio.play();error('');controls();}catch{say('Playback did not start. Press Resume to try again.');controls();}}
async function start(practice){
 const cloud=window.BC_CLASSROOM||{};if(active||!ready||cloud.initializing||cloud.mode==='teacher'||cloud.mode==='student'&&(cloud.submitting||cloud.closed||cloud.readOnly)||(!practice&&runs.length>=core.MAX_RUNS))return;
 if(personalRuns!==null){runs=personalRuns;personalRuns=null;}audio.pause();reviewUntil=null;active={id:uid(),clipId:clip.id,marks:[],exposed:priorExposure||revealed||runs.length>0||$('transcript-details').open,practice,position:0};audio.currentTime=0;lastSave=-1;$('comparison').hidden=true;$('round-title').textContent=practice?'Practice — first 15 seconds, not saved':`Participant ${cloud.participant||runs.length+1}`;
 priorExposure=true;log(practice?'practice_start':'start');window.dispatchEvent(new CustomEvent('bc-round-start'));
 say(practice?'Practice: mark when you would respond. These clicks will not enter the comparison.':'Listen and mark when you would respond.');persist();controls();await play();if(!$('mark').disabled)$('mark').focus();
}
function finish(){if(!active)return;audio.pause();const wasPractice=active.practice;log(wasPractice?'practice_complete':'complete');const count=active.marks.length;if(!wasPractice)runs.push(core.validateRun({...active,complete:true},clip));active=null;reviewUntil=null;if(!wasPractice)revealed=true;persist();renderComparison();$('round-title').textContent=wasPractice?'Practice complete':'Round complete';say(wasPractice?'Practice complete. Markers were not saved. Start your round when ready.':window.BC_CLASSROOM?.mode==='student'?`Saved ${count} markers. Submitting your round to the class activity…`:`Saved ${count} markers. Hand over to the next participant, or reveal the comparison when everyone is finished.`);controls();$(wasPractice?'start':'comparison-title').focus();if(!wasPractice)window.dispatchEvent(new CustomEvent('bc-round-complete',{detail:runs.at(-1)}));}
async function replay(from,to){if(active||!ready)return;audio.pause();reviewUntil=to;audio.currentTime=from;$('round-title').textContent='Review playback — markers are not recorded';say('Replaying nearby speech.');await play();}
function download(name,text,type){const url=URL.createObjectURL(new Blob([text],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);say('Download prepared. Check your browser’s downloads.');}
$('start').onclick=()=>start(false);$('practice').onclick=()=>start(true);
$('mark').onclick=()=>{if(!active||audio.paused||buffering||!ready)return;const t=+Math.min(audio.currentTime,clip.duration).toFixed(3);const prev=active.marks.at(-1);if(prev!==undefined&&t-prev<0.15)return;if(active.marks.length>=core.MAX_MARKS){say('This round has reached the 300-marker limit.');return;}active.marks.push(t);log('mark');persist();controls();};
$('mark').onkeydown=e=>{if(e.repeat&&(e.key===' '||e.key==='Enter'))e.preventDefault();};
$('undo').onclick=()=>{if(active){active.marks.pop();log('undo');persist();controls();say('Last marker removed.');}};
$('pause').onclick=()=>{if(audio.paused){log('resume');play();}else{log('pause');audio.pause();say('Paused. No markers are recorded while paused.');persist();controls();}};
$('discard').onclick=()=>{if(active&&confirm('Discard this unfinished round? Completed participants will be kept.')){log('discard');audio.pause();active=null;reviewUntil=null;persist();renderComparison();$('round-title').textContent='Ready for a new round';say('Unfinished round discarded.');controls();}};
$('reveal').onclick=()=>{if(active)return;revealed=true;persist();renderComparison();$('comparison-title').focus();};
$('clip').onchange=()=>{clip=clips[Number($('clip').value)];load();};
$('clear').onclick=()=>{if(!active&&confirm('Clear saved participants and reflection for this recording? Download your work first if you want to keep it.')){audio.pause();runs=[];active=null;revealed=false;$('reflection').value='';persist();renderComparison();say('Saved work for this recording cleared.');controls();}};
$('reflection').oninput=persist;
$('transcript-details').ontoggle=()=>{if($('transcript-details').open){priorExposure=true;persist();}log($('transcript-details').open?'transcript_open':'transcript_close');if(active&&!active.practice&&$('transcript-details').open){active.exposed=true;persist();}};
$('download').onclick=()=>download(`7A-${clip.id}-results.json`,JSON.stringify({format:'ling2150-7a',version:1,clipId:clip.id,duration:clip.duration,runs,reflection:$('reflection').value,source:clip.source,license:'CC BY 4.0'},null,2),'application/json');
$('csv').onclick=()=>{const own=window.BC_CLASSROOM?.mode==='student'&&personalRuns===null;let s='participant,local_attempt,record_id,clip_id,marker_seconds,prior_exposure\r\n';runs.forEach((r,i)=>{for(const t of r.marks.length?r.marks:[''])s+=`${own?window.BC_CLASSROOM.participant:r.participant||i+1},${own?i+1:""},${r.id},${clip.id},${t},${r.exposed}\r\n`;});download(`7A-${clip.id}-markers.csv`,s,'text/csv');};
$('import').onchange=async()=>{if(active)return;const files=[...$('import').files];const original=runs;const importClip=clip;try{if(files.length>60)throw Error('Import up to 60 files at a time.');let merged=runs;for(const f of files){if(f.size>1000000)throw Error('Each file must be smaller than 1 MB.');const content=await f.text();if(clip!==importClip||active)throw Error('The recording or round changed during import. Please try again.');merged=core.merge(merged,JSON.parse(content),clip);}runs=merged;persist();renderComparison();$('import-status').textContent=`Imported ${runs.length-original.length} new participants. Duplicate records were skipped.`;error('');}catch(e){error(`Import failed: ${e.message} Your existing results are unchanged.`);}finally{$('import').value='';controls();}};
$('retry-audio').onclick=()=>{if(active)active.position=audio.currentTime;ready=false;buffering=false;audio.load();say('Reloading recording…');controls();};
audio.addEventListener('canplaythrough',()=>{ready=true;buffering=false;$('retry-audio').hidden=true;error('');if(active&&Number.isFinite(active.position)){audio.currentTime=active.position;delete active.position;say('Your unfinished round is paused. Press Resume to continue.');}else if(!active&&reviewUntil===null)say('Ready. Start when you are ready to listen.');controls();});
audio.addEventListener('playing',()=>{buffering=false;ready=true;say(active?(active.practice?'Practice is playing.':'Playing. Mark when you would respond.'):'Review playback.');controls();});
audio.addEventListener('waiting',()=>{log('audio_waiting');buffering=true;say('Buffering — marker input is paused until the audio resumes.');controls();});
audio.addEventListener('pause',controls);
audio.addEventListener('error',()=>{log('audio_error');ready=false;buffering=false;error('The recording could not be loaded. Check your connection and reload it. Existing markers are kept.');$('retry-audio').hidden=false;controls();});
audio.addEventListener('timeupdate',()=>{const t=audio.currentTime;$('clock').textContent=`${time(t)} / ${time(clip.duration)}`;$('progress').value=t;if(active?.practice&&t>=15){finish();return;}if(reviewUntil!==null&&t>=reviewUntil){audio.pause();reviewUntil=null;say('Review paused. Choose another time to replay.');controls();}if(active&&!active.practice&&Math.floor(t)!==lastSave){lastSave=Math.floor(t);persist();}});
audio.addEventListener('ended',()=>{if(active)finish();else{reviewUntil=null;controls();}});
window.addEventListener('pagehide',()=>{log('page_leave');persist();});
window.BC_APP={clearView:()=>{audio.pause();active=null;runs=[];personalRuns=null;revealed=false;renderComparison();$('reflection').value='';},status:say,snapshot:()=>({clip,runs:personalRuns||runs,active:!!active,attemptId:active&&!active.practice?active.id:null}),controls,select:(id)=>{const next=clips.find(c=>c.id===id);if(!next)throw Error('This recording is not available.');clip=next;$('clip').value=clips.indexOf(clip);load();},compare:(records)=>{log('comparison_view');if(window.BC_CLASSROOM?.mode==='student'&&personalRuns===null)personalRuns=runs;runs=records.map(r=>core.validateRun(r,clip));revealed=true;renderComparison();$('comparison-title').focus();},last:()=>(personalRuns||runs).at(-1)};
clips.forEach((c,i)=>{const o=document.createElement('option');o.value=i;o.textContent=`${c.title} (${c.duration.toFixed(1)} seconds)`;$('clip').append(o);});load();
})();
